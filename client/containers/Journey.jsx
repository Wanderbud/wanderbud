import React from 'react';
import SearchTest from '../components/SearchTest';

const Posts = (props) => {
    return (
        <div>
            <SearchTest />
        </div>
    );
};

export default Post;